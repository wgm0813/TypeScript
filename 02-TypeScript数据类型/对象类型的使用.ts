type PointType = {x: number,y: number}
function prinCCoordinate(point: PointType){
  console.log(point.x)
  console.log(point.y)
}

// type PointType = {x:number, y:number}
prinCCoordinate({x:2,y:3})